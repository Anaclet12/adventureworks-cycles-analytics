# AdventureWorks Analytics API — service dependencies
# Pinned for reproducible Docker builds.

# --- Web framework ---
fastapi>=0.115,<1
uvicorn[standard]>=0.32,<1
pydantic>=2.9,<3
pydantic-settings>=2.6,<3

# --- ML / data (for live inference) ---
scikit-learn>=1.5,<2
joblib>=1.4,<2
pandas>=2.2,<3
numpy>=1.26,<3

# --- Monitoring ---
prometheus-fastapi-instrumentator>=7.0,<8

# --- Testing (dev only, kept here for the CI image) ---
pytest>=8.3,<9
httpx>=0.27,<1
